#include <iostream>

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}

/*
 * This is a test of the Git workflow, using new branches based off an initial 'empty' commit, containing only the base
 * project files and the standard "Hello, World!" boilerplate in a new CLion C++ project.
 *
 * This comment will should be removed in a subsequent commit, and will not be present in any other branches.
 */